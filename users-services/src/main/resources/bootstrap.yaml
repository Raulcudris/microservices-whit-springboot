Spring:
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  application:
    name: users-services
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/auth/realms/microservices-realm
